/*
void showMotorStatus() {
  Serial.print("leftMotorCurrentSpeed: ");
  Serial.print(leftMotorCurrentSpeed);
  Serial.print(". leftMotorSpeed[moveCounter]: ");
  Serial.println(leftMotorSpeed[moveCounter]);

  Serial.print("rightMotorCurrentSpeed: ");
  Serial.print(rightMotorCurrentSpeed);
  Serial.print(". rightMotorSpeed[moveCounter]: ");
  Serial.println(rightMotorSpeed[moveCounter]);

  Serial.println("");
}


void showUSStatus() {
  for (int i = 0; i < 5; i++) {
    Serial.println(uSInSight[i]);
  }
  Serial.println("");
}

void showCurrentMove() {
  Serial.print("currentMove: ");
  Serial.println(currentMove[moveCounter]);
}

void showMoveStatus() {
  Serial.print("currentMove: ");
  Serial.print(currentMove[moveCounter]);

  Serial.print(". moveDuration(ds): ");
  Serial.print(moveDuration[moveCounter]);

  Serial.print(". moveTimer(ds): ");
  Serial.print(moveTimer);

  Serial.print(". moveCounter: ");
  Serial.print(moveCounter);

  Serial.print(". amountOfMoves: ");
  Serial.println(amountOfMoves);
}

void showMoveQueue() {
  for (int i = 0; i < maxMoves; i++) {
    Serial.print("currentMove: ");
    Serial.print(currentMove[i]);

    Serial.print(". moveDuration(ds): ");
    Serial.print(moveDuration[i]);

    Serial.print(". moveTimer(ds): ");
    Serial.print(moveTimer);

    Serial.print(". straightSpeed: ");
    Serial.print(straightSpeed[i]);

    Serial.print(". rotationDirection: ");
    Serial.print(rotationDirection[i]);

    Serial.print(". rotationSpeed: ");
    Serial.print(rotationSpeed[i]);

    Serial.print(". turnSpeedLeft: ");
    Serial.print(turnSpeedLeft[i]);

    Serial.print(". turnSpeedRight: ");
    Serial.print(turnSpeedRight[i]);

    Serial.print(". moveTimer(ds): ");
    Serial.println(moveTimer);
  }
}

void showLineSensorStatus() {
  Serial.print("getLineSFrontRight: ");
  Serial.println(getLineSFrontRight());

  Serial.print("getLineSFrontLeft:  ");
  Serial.println(getLineSFrontLeft());

  Serial.print("getLineSBackRight:  ");
  Serial.println(getLineSBackRight());

  Serial.print("getLineSBackLeft:   ");
  Serial.println(getLineSBackLeft());

  Serial.println("");
}
*/


